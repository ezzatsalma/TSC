let message = " 'you are invited to dinner'";

let names :string[] = ['AMMAR', 'AMMARA', 'HAFSA'];
let quantity = names.push('NAILA');
names.unshift('Fatima')
names.push('Maria')
names.splice(names.length/2,0, "salma")

console.log('I found a bigger dinner table');
names.forEach(names=>{
    console.log("dear  "names " join for dinner" )
    )}


